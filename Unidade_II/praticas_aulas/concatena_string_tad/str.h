/*Funções oferecidas pelo str.c*/

/*Função comprimento:
**retorna o número de caracteres da string passada como parâmetro.
*/
int comprimento(char *str);

/*Função copia:
**copia os caracteres da string orig(origem) para dest(destino).
*/
void copia(char *dest, char *orig);

/*Função concatena:
**concatena a string orig(origem) com a dest(destino).
*/
void concatena(char *dest, char *orig);
